using MauiIcons.Core;

namespace PlexManager.Components.Blocks
{
    public partial class Libraries : ContentView
    {
        public Libraries()
        {
            InitializeComponent();

            _ = new MauiIcon();
        }
    }
}
